# Gordo Portal (Streamlit) – Gemini + CSV
Run locally:
```
pip install -r requirements.txt
export GEMINI_API_KEY=YOUR_KEY_HERE
streamlit run app.py
```
On Streamlit Cloud: add secret `gemini_api_key`.
